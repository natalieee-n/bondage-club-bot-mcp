services:
  mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bondage-club-bot-mcp
    restart: always
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    working_dir: /app
    command: python server.py
